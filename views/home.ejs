<%- include('partials/header') %>

    <h1>Home</h1>
    <p>
        <%=homeStartingContent%>
    </p>

    <!-- <% for(var i=0; i<posts.length; i++){ %> 
        <p><%= posts[i].title %></p>
        <p><%= posts[i].body %></p>
        <% } %> -->
    <!-- OR  -->
    <% posts.forEach((post)=> { %>

        <h1>
            <%= post.title %>
        </h1>
        <p>
            <%= post.body.substring(0, 100) + " ..." %> 
            <a href="/posts/<%=post.title%>" style="color: #1abc9c;" >Read More</a>
        </p>

        <% }); %>


        <a href="/compose" class="btn btn-primary btn-lg btn-compose">
             <strong>Compose +</strong>
        </a>

<%- include('partials/footer') %>